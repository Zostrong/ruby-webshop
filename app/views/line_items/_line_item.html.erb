<tr>
  <td class="col-sm-8 col-md-6">
    <div class="media">
      <a class="thumbnail pull-left" href="#"> <img class="media-object" src="<%= line_item.product.image %>" style="width: 72px; height: 72px;"> </a>
      <div class="media-body">
        <h4 class="media-heading"><a href="<%= product_path(line_item.product) %>"><%= line_item.product.model %></a></h4>
        <h5 class="media-heading"><%= line_item.product.brand %></h5>
      </div>
    </div></td>
  <td class="col-sm-1 col-md-1 text-center"><strong><%= line_item.quantity %></strong></td>
  <td class="col-sm-1 col-md-1 text-center"><strong><%= number_to_currency(line_item.product.price, unit: "Ft", precision: 0, format: "%n %u") %></strong></td>
  <td class="col-sm-1 col-md-1 text-center"><strong><%= number_to_currency(line_item.total_price, unit: "Ft", precision: 0, format: "%n %u") %></strong></td>
  <td class="col-sm-1 col-md-1">
    <div>
      <span class="glyphicon glyphicon-remove"></span>
      <%= link_to "Törlés", line_item, method: :delete, data: { confirm: "Biztos törölni akarja a terméket? "}, class:"btn btn-danger" %>
    </div>
</tr>